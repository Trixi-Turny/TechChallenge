


<a href="#">Back to the start</a>

            <table id="form_table">
                <tr class="form_table_row">
                    <th class="form_table_head"></th>
                    <th class="form_table_head">
                    <form id = "dynamicForm" action="/" method="post">
                        <fieldset>
                            <a class="storyLink" href="">{{storyLine0}}</a>
                            {{#if storyLine0}}{{else}}<textarea class = "textarea" name= "textarea0">{{textBoxValue}}</textarea>
                            <input type="submit" value="Submit" id = "submit">{{/if}}
                        </fieldset>
                        </form>
                    </th>
                    <th class="form_table_head"></th>
                </tr>
                    <tr class="form_table_row">
                      <th class="form_table_head">
                      <form id = "dynamicForm" action="/" method="post">
                        <fieldset>
                            <a class="storyLink" href="">{{storyLine1}}</a>
                            {{#if storyLine1}}{{else}}<textarea class = "textarea" name = "textarea1">{{textBoxValue}}</textarea>
                            <input type="submit" value="Submit" >{{/if}}
                        </fieldset>
                        </form>
                    </th>
                    <th class="form_table_head">
                        <p class="centerStory storyLink">{{centerStory}}</p>
                    </th>
                     <th class="form_table_head">
                     <form id = "dynamicForm" action="/" method="post">
                        <fieldset>
                            <a class="storyLink" href="">{{storyLine2}}</a>
                            {{#if storyLine2}}{{else}}<textarea class = "textarea" name = "textarea2">{{textBoxValue}}</textarea>
                            <input type="submit" value="Submit">{{/if}}
                        </fieldset>
                        </form>
                    </th>
                </tr>
                <tr class="form_table_row">
                    <th class="form_table_head"></th>
                    <th class="form_table_head">
                    <form id = "dynamicForm" action="/" method="post">
                        <fieldset>
                            <a name = "simon" class="storyLink" href="">{{storyLine3}}</a>
                            {{#if storyLine3}}{{else}}<textarea class = "textarea" name = "textarea3">{{textBoxValue}}</textarea>
                            <input type="submit" value="Submit">{{/if}}
                        </fieldset>
                        </form>
                    </th>
                    <th class="form_table_head"></th>
                </tr>
            </table>



